#include "../../../../../include/gameStates/level/LevelHeaders.h"

LivingObject::LivingObject(GameObjectName objectName, Coordinates position, class Level *level)
        : MovingObject(objectName, position, level)
{
    ;
}